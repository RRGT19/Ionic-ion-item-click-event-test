import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-ion-item-click-event',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
